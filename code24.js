function duplicateCount(text) {
  const container = [];
  const buscador = text.filter((x) => (x === "a" ? x.length : ""));

  return buscador;
}

console.log(duplicateCount(["a", "d", "f", "a", "a"]));
